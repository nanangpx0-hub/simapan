// Font Awesome (ikon sidebar & header AdminLTE)
import '@fortawesome/fontawesome-free/css/all.css';

// Bootstrap 5 JS (dropdown, modal, dsb. — tanpa jQuery)
import 'bootstrap';

// AdminLTE 4 JS (data-api: push menu sidebar, treeview, dsb.)
import 'admin-lte';

import './bootstrap';

